export interface WelcomeImageOptions {
  username: string;
  avatarUrl: string;
  backgroundUrl: string;
  hexColor: string;
  quote: string | null;
  guildName: string;
  guildIconUrl?: string | undefined;
}
